{
  "name": "chain_medics",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ chain_medics",
    "create-local": "graph create --node http://localhost:8020/ chain_medics",
    "remove-local": "graph remove --node http://localhost:8020/ chain_medics",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 chain_medics",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.57.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
